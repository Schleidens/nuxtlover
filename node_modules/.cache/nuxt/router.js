import Vue from 'vue'
import Router from 'vue-router'
import { normalizeURL, decode } from 'ufo'
import { interopDefault } from './utils'
import scrollBehavior from './router.scrollBehavior.js'

const _69f68ffd = () => interopDefault(import('../../../pages/projects/index.vue' /* webpackChunkName: "pages/projects/index" */))
const _683feeb5 = () => interopDefault(import('../../../pages/projects/_slug.vue' /* webpackChunkName: "pages/projects/_slug" */))
const _64b8c6fc = () => interopDefault(import('../../../pages/index.vue' /* webpackChunkName: "pages/index" */))
const _6826098c = () => interopDefault(import('../../../pages/_slug.vue' /* webpackChunkName: "pages/_slug" */))

const emptyFn = () => {}

Vue.use(Router)

export const routerOptions = {
  mode: 'history',
  base: '/',
  linkActiveClass: 'nuxt-link-active',
  linkExactActiveClass: 'nuxt-link-exact-active',
  scrollBehavior,

  routes: [{
    path: "/projects",
    component: _69f68ffd,
    name: "projects"
  }, {
    path: "/projects/:slug",
    component: _683feeb5,
    name: "projects-slug"
  }, {
    path: "/",
    component: _64b8c6fc,
    name: "index"
  }, {
    path: "/:slug",
    component: _6826098c,
    name: "slug"
  }],

  fallback: false
}

export function createRouter (ssrContext, config) {
  const base = (config._app && config._app.basePath) || routerOptions.base
  const router = new Router({ ...routerOptions, base  })

  // TODO: remove in Nuxt 3
  const originalPush = router.push
  router.push = function push (location, onComplete = emptyFn, onAbort) {
    return originalPush.call(this, location, onComplete, onAbort)
  }

  const resolve = router.resolve.bind(router)
  router.resolve = (to, current, append) => {
    if (typeof to === 'string') {
      to = normalizeURL(to)
    }
    return resolve(to, current, append)
  }

  return router
}
